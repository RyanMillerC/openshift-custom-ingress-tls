apiVersion: redhatcop.redhat.io/v1alpha1
kind: Patch
metadata:
  name: ingress-custom-tls
  namespace: openshift-config
spec:
  serviceAccountRef:
    name: default
  patches:
    ingress-custom-tls:
      targetObjectRef:
        apiVersion: operator.openshift.io/v1
        kind: IngressController
        name: default
      patchTemplate: |
        spec:
          defaultCertificate:
            name: ingress-custom-tls
      patchType: application/merge-patch+json
